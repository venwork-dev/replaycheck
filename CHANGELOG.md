# Changelog

## 0.2.0

- Documented the supported Python versions and public API contract.
- Added `replaycheck --version`.
- Added machine-readable CLI reports with `--json`.
- Added `--fail-on-partial` for strict CI coverage policies.
- Added deterministic generative coverage for schedule generation, event
  isolation, and setup state.
- Added a dependency-free scaling benchmark and CI budget guidance.

## 0.1.0

- Initial release of replay/crash schedule checking for at-least-once handlers.
